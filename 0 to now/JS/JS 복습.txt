21.02.16
alert 경고 문구
문자와 숫자 구분

문자는 "" 나 ''  안에 넣어준다

숫자 그냥 써주고 연산 +-*/떄 값이 숫자로 나온다.

indexOf
"code".indexOf("e")
3
몇번째에 있는가 (처음 출발은 0이다.)
"asfs".length
4
문자의 갯수

변수
변수는 코드의 재활용성을 높혀준다.
var
var a = 1;
var first = "coding";
결과 값으로 숫자뿐만 아니라 문자 또한 들어갈 수 있다.
처음 변수를 사용할 때  var를 써주고
다음에 쓸 땐 그냥 써도 된다. ( 그런데 그냥 처음아니더라도 그냥 써도 된다. 관습적인 것일듯?)

주석
//   뒤에는 주석이다.
문법적 해석 안함.
줄바꿈 전까지가 내용
/*

*/
여러줄 주석을 사용할 때 사용
자신의 코드 취지를 설명할 필요가 있을 때 사용
하지만 정말 좋은 코드는 주석조차 필요없는 것.
어떤 코드를 일시적으로 사용하지 않을때 또한 주석을 사용

자바스크립트는 줄이 끝나면 명령이 끝난걸로 인식하지만, 그래도 ;(세미콜론)
하는게 좋다.

비교

연산자
대입연산자
=
동등 연산자 
== (equal operator)  데이터 형식이 다르더라도 정보의 의미가 동일하면 true
일치 연산자
=== (strict equal operator) 데이터 형식까지 같아야 함

일치 연산자를 사용하는 것을 권고함
더 정확하기때문에 에러요소가 생기는 것을 줄일 수 있다.

21.02.17
undefined 정의되지 않음
null 값이 없음



JS에서는 동등연사자(==)는 숫자 1을 true 라 한다.
0은 false 이외에도 '' (빈문자열)

!=
!는 부정을 의미한다.

조건문
비교와 연관
Boolean  true/false  (조건문과 밀접한 관계)
조건문
if(true) { } 조건문이 true이면 { } 안에 내용이 출력이 되고
아니면 그 뒤 내용이 출력된다.

prompt 
질문을 적어넣은 것으로 사용자로부터 정보를 받을 수 있다.

아이디를 입력해주세요 예제 연습 다시 해보기
비밀번호까지

논리 연산자
&& 는 and 를 의미한다.
|| 는 or 를 의미한다.

수학에서와 같이 괄호값이 먼저 계산된다.
ex)   if((id=korea) && password = '1234')  id먼저 그 후 password

!는 not을 의미한다.

      id = prompt('아이디를 입력해주세요');
      password = prompt('비밀번호를 입력해주세요');
      if((id=='hye' || id == 'just'|| id == 'ok') && password='1234'){
        alert('you are ok');
      }
      else{
        alert(not ok);
      }
실행 안되는 이유를 잘 모르겠음
console에서는  if 부분에 문제가 있다고 떳지만...

반복문(loop, iterate)

document.write(" ");
html과 같이 문자를 출력해놓을 수 있다. 페이지안에

다시 반복문
코드
while(조건) {
            반복해서 실행할 코드
}

21.02.18
while 문의 단점 더 많은 라인과 그 과정중에 오류가 생길 수 있다.

그래서 for문 
사용
for(i=0; i < 10, i++;) { }
순서 중요하다. 3번째랑 2번째 조건 순서가 바뀌면 무한대.
i의 값 반복조건 반복이 될 때마다 실행되는 코드
i = i+1;
i++;
거의 같은 의미이다.
i++    i=0;  alert(i++);  하면 0
++i 차이     i=0; alert(++i); 하면 1
i먼저 넣고(출력하고) 더하기
더하고 아이넣기 이런 차이이다.

break와 continue 차이
break는 반복문을 멈추고
continue는 조건상황에서 멈추고 다시 반복문 재게
if 절과 함께 쓰는 예제 다시 해보기

반복문 중첩
자바스크립트 내에서
문자 + 숫자
는 숫자 부분을 문자로 파악해서
문자 + 문자 가 된다.
i와 j 로 for 문 중첩
deburger로 코드가 실행되는 과정을 볼 수 있다.

함수        함수의 이름
function numbering() { 조건 }
함수또한 재사용성이 높아진다.
재사용성 유지보수의 용이 가독성

출력 
return
function { return '~~'; }
로 나타낸다.

입력값은 여러개 나타낼 수 있지만
return값은 하나만  갖게된다.

function get_argument(arg){                   여기로 가서 (매개변수)
           return arg; }                                           다시 여기서 값으로 나옴
alert (get_argument(1));                    여기 1이        (인자)

numbering = function(){ ~~ }
numbering();
함수를 다르게 정의하는 방법
자바스크립트에서 함수를 정의하는 방법은 여러가지다. 

익명함수
(함수)  이 형태 
이름이 필요없고 바로 사용할때,일회성으로 사용할때 주로 사용

배열 array
연관된 데이터 모아서 통으로 사용하는 데이터 타입
데이터를 통으로 담는 것
var member= [ '1', '2', '3' ];


alert(member[0]);    >>>> 1

함수정의에서 arg제외하고 그냥 (0) (1) (2)
부를수 있나 몰랐음 다시 찾아보기

함수,배열과 반복문 예제

21.02.19
배열 
데이터 추가
push
사용은 li.push('문자');
push는 원본 배열을 직접 변경
인수 전달받은 배열 그대로 삽입된다.
다른 방법으론 연결
concat
li = li.concat 사용
원본 배열을 변경하지 않고 새로운 배열
인수받은 걸 해체하고 원래 배열에 맞춘다.

원래 배열 뒤쪽에 추가하는 방법들

배열 앞쪽에 추가하는 방법
unshift
li.unshift('문자');

중간에 넣는 방법
splice 사용
몇번째에 삽입
거기부터(시작점) 몇개를 지움
삽입할 문자
array.slice(2, 2, 'a' 'b')
배열에서 2번째 문자부터 2개 문자 지우고 a b 삽입;

제거
shift 를 사용한다
li.shift(); 제일 앞에 있는 요소 제거
li.pop(); 제일 뒤에 있는 요소 제거

정렬 
li.sort(); 
알파벳 순 순자 순 등
역순
li.reverse(); 사용

객체(object)
배열과 유사하지만, 배열이 0 1 2 로 인덱스라면
객체는 인덱스를 설정할 수 있다.   (가시적인 차이)
var grade = {'hey' : 10, 'mr': 7, 'example': 5}
                key     value
grade['hey']
10

객체부분 다시 봐야됨


21.02.21
poiemaweb 복습
함수란 이런거구나...
함수를 설정해두고
function 이름(함수선언문) {
            return 어떻게 정의할지;
 }

나중에 

함수이름( 값);
값이 나옴
그래서 이미 정의해놓은 함수를 쉽게 쓸 수 있다.
>>>>재활용성 재사용 측면 유용의 의미

switch 문 

switch (표현식) { case 표현식1: 일치하면 실행될 문; break; ~~~
            default: 위 case 일치하는 식 없을 때 실행될 문; }

21.02.25
js 생활코딩 복습
< 
js 에서 꺽새로 잘못 인식할 수 있어서
&lt;
less than ~

생활코딩 배열
배열은  [ ] 
대괄호로 시작하고 끝난다.
순서대로 연관된 데이터 정리전달

반복문
배열과 함께 사용되는 반복문
loop로 사용된다.
반복문 기본 문법 while
배열에 담겨있는 데이터를 꺼내 자동화된 처리

querySelector 
는 전체중 첫번째것만 가져온다.

21.02.26
함수

function name(파라미터) {함수정의 }
                   매개변수
name(argument)
argument값이 파라미터로 들어가서 함수 정의에 맞게 계산되어 나온다.

return 의미
함수를 계산을 하는 값의 용도로만 사용한다.
예시
function ex(a, b){
 return a+b; }
ex(2+3); 

하면 아무 값도 나오지 않는다.
document.write(ex(2,3));

해야 5가 나온다.
<script> 내에서

함수는 리팩토링에서 중요한 역할을 한다.

객체 object
객체에 속해있는 함수는 method라고 부른다.
배열과 다르게 순서없이 정보를 저장
(배열은 중괄호 객체는 대괄호)
배열내에 객체를 추가할떄
기본적으로는
배열.객체이름 = "무슨이름";
하면 추가되지만
띄어쓰기를 하고 싶을땐

배열[객체 띄어쓰기]= "띄어쓰기이름";
해주면 된다.
객체에서는 key
배열에서는 index라는 표현으로 쓴다.(순서대로 정리되어 있어서.)
객체에 소속된 변수 = 프로퍼티
객체에 소속된 함수 = 메소드 (일반함수들과 구분하기 위해서)
객체의 프로퍼티와 프로퍼티 구분할땐 , 사용

정리
함수는 코드 많아지는 걸 정리 정돈
객체는 함수와 변수 많아진걸 그룹핑

21.02.27
library    framework

라이브러리 가져다 씀
프레임워크 기본 틀이 있고 거기서 덧 붙혀 만드는 것.
UI   user interface
유저가 사용하는 걸 만들때
API  Application programming interface
프로그래밍을 하는 사람을 위해 만들어진 프로그램

생활코딩 마무리.

검색 개체
document
dom 
window 

webGL 3차원 그래픽

전체  long take 하는 부분에서
함수부분 부터 내가 작성 못 할 것 같다.
jquery 문법 잘 모르겠음 왜 저렇게 사용되고 그 의미가 무엇인지.
물론 제이쿼리 요즘 잘 안 쓴다하지만.

21.03.01
코딩테스트 연습 가운데 글자 가져오기
짝수랑 홀수일때를 나눠서 생각을 했고
짝수일때 %2 했을때 0으로 조건을 두었고

if(str.length %2) { 홀수조건 ... 글자에서charAt 가운데=str.length/2 + 0.5
        else 짝수조건 str.substring(str.length/2, 2);
                 글자길이 절반위치에서 그 글자 포함 앞 하나 더까지
                   >>>>>가운데 두글자
로 설정을 해두었다.

함수값이 s에 글자를 넣어서 실해하려고 했으나
실행이 안된다. 글자 설정이 안되어있다고 계속 나옴
함수 내의 변수 설정과 answer  return값을 설정이 제대로 안된것 같다.